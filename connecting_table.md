          AWS Cloud (Console)
        ┌─────────────────────────┐
        │ - S3 (files)            │
        │ - Lambda (functions)    │
        │ - DynamoDB (database)   │
        │ - Cognito (users)       │
        └─────────────────────────┘
                    ▲
                    │ (AWS CLI / Amplify CLI)
                    │
 VS Code (local) ───────────→   GitHub (remote code)
